# Build outputs
*.exe
*.dll
*.so
*.dylib

# Test outputs
*.test
*.out
coverage.txt
*.prof

# Env vars
.env

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Local environment
.env
.env.local

# Vendor (we don't vendor)
vendor/

# Claude Code tooling (sub-agent worktrees, etc.)
.claude/
kms-keys.json
kms-keys.local.json

# examples/conversations runtime artifacts (chat CLI default --db and
# its sidecar KMS file). SQLite WAL/SHM journal files included.
chat.db
chat.db-shm
chat.db-wal
chat.db.kms.json
*.kms.json
