// Code generated by protoc-gen-es-go. DO NOT EDIT.
// source: myapp/conversation/v1/conversation.proto

package conversationv1

import (
	context "context"
	base64 "encoding/base64"
	errors "errors"
	fmt "fmt"
	aggregate "github.com/laenenai/eventstore/aggregate"
	es "github.com/laenenai/eventstore/es"
	projection "github.com/laenenai/eventstore/projection"
	shred "github.com/laenenai/eventstore/shred"
	proto "google.golang.org/protobuf/proto"
	slog "log/slog"
)

// Command is the sealed interface for the Commands sum type.
// Variants implement this via the unexported isCommand() marker method.
type Command interface {
	isCommand()
}

func (*Start) isCommand()                  {}
func (*AppendUserMessage) isCommand()      {}
func (*AppendAssistantMessage) isCommand() {}
func (*Close) isCommand()                  {}

// CloneSum returns a deep copy of m typed as the sealed Command interface.
// Delegates to the typed Clone(); exists so generic callers can satisfy a
// `Cloner[Command]` interface assertion (see aggregate.Runtime).
func (m *Start) CloneSum() Command { return m.Clone() }

// CloneSum returns a deep copy of m typed as the sealed Command interface.
// Delegates to the typed Clone(); exists so generic callers can satisfy a
// `Cloner[Command]` interface assertion (see aggregate.Runtime).
func (m *AppendUserMessage) CloneSum() Command { return m.Clone() }

// CloneSum returns a deep copy of m typed as the sealed Command interface.
// Delegates to the typed Clone(); exists so generic callers can satisfy a
// `Cloner[Command]` interface assertion (see aggregate.Runtime).
func (m *AppendAssistantMessage) CloneSum() Command { return m.Clone() }

// CloneSum returns a deep copy of m typed as the sealed Command interface.
// Delegates to the typed Clone(); exists so generic callers can satisfy a
// `Cloner[Command]` interface assertion (see aggregate.Runtime).
func (m *Close) CloneSum() Command { return m.Clone() }

// Stable per-variant identifiers — full proto type names.
// Useful for authz, metrics, tracing, and audit annotation.
func (*Start) Action() string                  { return "myapp.conversation.v1.Start" }
func (*AppendUserMessage) Action() string      { return "myapp.conversation.v1.AppendUserMessage" }
func (*AppendAssistantMessage) Action() string { return "myapp.conversation.v1.AppendAssistantMessage" }
func (*Close) Action() string                  { return "myapp.conversation.v1.Close" }

// CommandCodec encodes and decodes Command variants on the wire.
// Implements aggregate.Codec[Command].
type CommandCodec struct{}

// Compile-time assertion that the codec satisfies the framework contract.
var _ aggregate.Codec[Command] = CommandCodec{}

// Encode marshals v as proto bytes and produces an EncodedEvent
// tagged with the variant's full proto type name.
func (CommandCodec) Encode(v Command) (aggregate.EncodedEvent, error) {
	switch x := v.(type) {
	case *Start:
		b, err := proto.Marshal(x)
		if err != nil {
			return aggregate.EncodedEvent{}, err
		}
		return aggregate.EncodedEvent{
			Payload:       b,
			TypeURL:       "myapp.conversation.v1.Start",
			SchemaVersion: 1,
		}, nil
	case *AppendUserMessage:
		b, err := proto.Marshal(x)
		if err != nil {
			return aggregate.EncodedEvent{}, err
		}
		return aggregate.EncodedEvent{
			Payload:       b,
			TypeURL:       "myapp.conversation.v1.AppendUserMessage",
			SchemaVersion: 1,
		}, nil
	case *AppendAssistantMessage:
		b, err := proto.Marshal(x)
		if err != nil {
			return aggregate.EncodedEvent{}, err
		}
		return aggregate.EncodedEvent{
			Payload:       b,
			TypeURL:       "myapp.conversation.v1.AppendAssistantMessage",
			SchemaVersion: 1,
		}, nil
	case *Close:
		b, err := proto.Marshal(x)
		if err != nil {
			return aggregate.EncodedEvent{}, err
		}
		return aggregate.EncodedEvent{
			Payload:       b,
			TypeURL:       "myapp.conversation.v1.Close",
			SchemaVersion: 1,
		}, nil
	}
	return aggregate.EncodedEvent{}, fmt.Errorf("CommandCodec.Encode: unknown variant type %T", v)
}

// Decode unmarshals the proto payload back into the variant identified
// by typeURL. The schemaVersion is passed through for upcaster
// dispatch (ADR 0013); this generated codec does not perform upcasting.
func (CommandCodec) Decode(typeURL string, _ uint32, payload []byte) (Command, error) {
	switch typeURL {
	case "myapp.conversation.v1.Start":
		x := &Start{}
		if err := proto.Unmarshal(payload, x); err != nil {
			return nil, err
		}
		return x, nil
	case "myapp.conversation.v1.AppendUserMessage":
		x := &AppendUserMessage{}
		if err := proto.Unmarshal(payload, x); err != nil {
			return nil, err
		}
		return x, nil
	case "myapp.conversation.v1.AppendAssistantMessage":
		x := &AppendAssistantMessage{}
		if err := proto.Unmarshal(payload, x); err != nil {
			return nil, err
		}
		return x, nil
	case "myapp.conversation.v1.Close":
		x := &Close{}
		if err := proto.Unmarshal(payload, x); err != nil {
			return nil, err
		}
		return x, nil
	}
	return nil, fmt.Errorf("CommandCodec.Decode: unknown type_url %q", typeURL)
}

// Event is the sealed interface for the Events sum type.
// Variants implement this via the unexported isEvent() marker method.
type Event interface {
	isEvent()
}

func (*Started) isEvent()                  {}
func (*UserMessageAppended) isEvent()      {}
func (*AssistantMessageAppended) isEvent() {}
func (*Closed) isEvent()                   {}

// CloneSum returns a deep copy of m typed as the sealed Event interface.
// Delegates to the typed Clone(); exists so generic callers can satisfy a
// `Cloner[Event]` interface assertion (see aggregate.Runtime).
func (m *Started) CloneSum() Event { return m.Clone() }

// CloneSum returns a deep copy of m typed as the sealed Event interface.
// Delegates to the typed Clone(); exists so generic callers can satisfy a
// `Cloner[Event]` interface assertion (see aggregate.Runtime).
func (m *UserMessageAppended) CloneSum() Event { return m.Clone() }

// CloneSum returns a deep copy of m typed as the sealed Event interface.
// Delegates to the typed Clone(); exists so generic callers can satisfy a
// `Cloner[Event]` interface assertion (see aggregate.Runtime).
func (m *AssistantMessageAppended) CloneSum() Event { return m.Clone() }

// CloneSum returns a deep copy of m typed as the sealed Event interface.
// Delegates to the typed Clone(); exists so generic callers can satisfy a
// `Cloner[Event]` interface assertion (see aggregate.Runtime).
func (m *Closed) CloneSum() Event { return m.Clone() }

// Stable per-variant identifiers — full proto type names.
// Useful for authz, metrics, tracing, and audit annotation.
func (*Started) Action() string             { return "myapp.conversation.v1.Started" }
func (*UserMessageAppended) Action() string { return "myapp.conversation.v1.UserMessageAppended" }
func (*AssistantMessageAppended) Action() string {
	return "myapp.conversation.v1.AssistantMessageAppended"
}
func (*Closed) Action() string { return "myapp.conversation.v1.Closed" }

// EventCodec encodes and decodes Event variants on the wire.
// Implements aggregate.Codec[Event].
type EventCodec struct{}

// Compile-time assertion that the codec satisfies the framework contract.
var _ aggregate.Codec[Event] = EventCodec{}

// Encode marshals v as proto bytes and produces an EncodedEvent
// tagged with the variant's full proto type name.
func (EventCodec) Encode(v Event) (aggregate.EncodedEvent, error) {
	switch x := v.(type) {
	case *Started:
		b, err := proto.Marshal(x)
		if err != nil {
			return aggregate.EncodedEvent{}, err
		}
		return aggregate.EncodedEvent{
			Payload:       b,
			TypeURL:       "myapp.conversation.v1.Started",
			SchemaVersion: 1,
		}, nil
	case *UserMessageAppended:
		b, err := proto.Marshal(x)
		if err != nil {
			return aggregate.EncodedEvent{}, err
		}
		return aggregate.EncodedEvent{
			Payload:       b,
			TypeURL:       "myapp.conversation.v1.UserMessageAppended",
			SchemaVersion: 1,
		}, nil
	case *AssistantMessageAppended:
		b, err := proto.Marshal(x)
		if err != nil {
			return aggregate.EncodedEvent{}, err
		}
		return aggregate.EncodedEvent{
			Payload:       b,
			TypeURL:       "myapp.conversation.v1.AssistantMessageAppended",
			SchemaVersion: 1,
		}, nil
	case *Closed:
		b, err := proto.Marshal(x)
		if err != nil {
			return aggregate.EncodedEvent{}, err
		}
		return aggregate.EncodedEvent{
			Payload:       b,
			TypeURL:       "myapp.conversation.v1.Closed",
			SchemaVersion: 1,
		}, nil
	}
	return aggregate.EncodedEvent{}, fmt.Errorf("EventCodec.Encode: unknown variant type %T", v)
}

// Decode unmarshals the proto payload back into the variant identified
// by typeURL. The schemaVersion is passed through for upcaster
// dispatch (ADR 0013); this generated codec does not perform upcasting.
func (EventCodec) Decode(typeURL string, _ uint32, payload []byte) (Event, error) {
	switch typeURL {
	case "myapp.conversation.v1.Started":
		x := &Started{}
		if err := proto.Unmarshal(payload, x); err != nil {
			return nil, err
		}
		return x, nil
	case "myapp.conversation.v1.UserMessageAppended":
		x := &UserMessageAppended{}
		if err := proto.Unmarshal(payload, x); err != nil {
			return nil, err
		}
		return x, nil
	case "myapp.conversation.v1.AssistantMessageAppended":
		x := &AssistantMessageAppended{}
		if err := proto.Unmarshal(payload, x); err != nil {
			return nil, err
		}
		return x, nil
	case "myapp.conversation.v1.Closed":
		x := &Closed{}
		if err := proto.Unmarshal(payload, x); err != nil {
			return nil, err
		}
		return x, nil
	}
	return nil, fmt.Errorf("EventCodec.Decode: unknown type_url %q", typeURL)
}

// Projection is the typed handler interface for the events in
// this package. Implementations must provide a method for every
// variant — adding a new event will break implementations at
// compile time until they decide how to handle it (return nil
// is fine for variants the projection deliberately ignores).
// See ADR 0020 decisions 3a + 3b.
type Projection interface {
	OnStarted(ctx context.Context, env es.Envelope, e *Started) error
	OnUserMessageAppended(ctx context.Context, env es.Envelope, e *UserMessageAppended) error
	OnAssistantMessageAppended(ctx context.Context, env es.Envelope, e *AssistantMessageAppended) error
	OnClosed(ctx context.Context, env es.Envelope, e *Closed) error
}

// NewProjectionDispatcher returns a projection.Handler that decodes
// envelopes carrying this package's events and dispatches to the
// typed Projection interface. By default an event whose TypeURL is
// outside this aggregate's event set causes a non-nil error; use
// projection.IgnoreUnknown() when composing across aggregates via
// projection.Chain.
func NewProjectionDispatcher(p Projection, opts ...projection.DispatcherOption) projection.Handler {
	cfg := projection.ApplyOptions(opts)
	var codec EventCodec
	return func(ctx context.Context, env es.Envelope) error {
		if !isOurType(env.TypeURL) {
			if cfg.IgnoreUnknown {
				return nil
			}
			return fmt.Errorf("projection: unknown TypeURL %q for package myapp.conversation.v1", env.TypeURL)
		}
		evt, err := codec.Decode(env.TypeURL, env.SchemaVersion, env.Payload)
		if err != nil {
			return fmt.Errorf("projection: decode %s: %w", env.TypeURL, err)
		}
		switch e := evt.(type) {
		case *Started:
			return p.OnStarted(ctx, env, e)
		case *UserMessageAppended:
			return p.OnUserMessageAppended(ctx, env, e)
		case *AssistantMessageAppended:
			return p.OnAssistantMessageAppended(ctx, env, e)
		case *Closed:
			return p.OnClosed(ctx, env, e)
		}
		return fmt.Errorf("projection: unreachable variant %T", evt)
	}
}

// isOurType reports whether the given TypeURL is one of this
// package's event variants. Used by the dispatcher to decide
// between dispatch and (skip|error) for unknown events.
func isOurType(typeURL string) bool {
	switch typeURL {
	case "myapp.conversation.v1.Started":
		return true
	case "myapp.conversation.v1.UserMessageAppended":
		return true
	case "myapp.conversation.v1.AssistantMessageAppended":
		return true
	case "myapp.conversation.v1.Closed":
		return true
	}
	return false
}

// ---- Crypto-shredding for UserMessageAppended (ADR 0010) ----

// PIIFields returns the proto field names of UserMessageAppended's
// encrypted fields. Stable across regenerations.
func (*UserMessageAppended) PIIFields() []string {
	return []string{
		"content",
	}
}

// Subject returns the default subject id used to key the DEK
// for this event's PII fields. Empty when the variant has no
// (es.v1.subject_field) — caller falls back to the StreamID.
func (e *UserMessageAppended) Subject() string {
	return e.GetUserId()
}

// EncryptPII encrypts every classification-PERSONAL+ field in
// place using s. Called by aggregate.Runtime before Codec.Encode
// when Runtime.Shredder is configured. The framework passes the
// resolved subject id; per-field (es.v1.subject) overrides are
// honored below.
//
// `bytes` fields hold raw ciphertext after encrypt; `string` fields
// classified PERSONAL+ hold base64-encoded ciphertext so the field
// remains UTF-8-valid. Fields classified
// (es.v1.data_classification) = DATA_CLASSIFICATION_SAD cause this
// method to return an error before any field is touched — SAD MUST
// NOT be persisted (PCI-DSS §3.2). See ADR 0027.
func (e *UserMessageAppended) EncryptPII(ctx context.Context, s *shred.Shredder, tenantID, subject string) error {
	if e.Content != "" {
		sealed, err := s.EncryptField(ctx, tenantID, subject, []byte(e.Content))
		if err != nil {
			return fmt.Errorf("UserMessageAppended.EncryptPII content: %w", err)
		}
		e.Content = base64.RawStdEncoding.EncodeToString(sealed)
	}
	return nil
}

// DecryptPII reverses EncryptPII. Per-field shred → RedactedField;
// base64 decode failure on string PII fields counts as a corrupt
// payload and aborts; other errors abort. Reading a SAD-classified
// field from persistence is itself corruption (it should never have
// been written) and returns the same SAD reject error.
func (e *UserMessageAppended) DecryptPII(ctx context.Context, s *shred.Shredder, tenantID, subject string) (shred.RedactedFields, error) {
	var redacted shred.RedactedFields
	if e.Content != "" {
		sealed, b64err := base64.RawStdEncoding.DecodeString(e.Content)
		if b64err != nil {
			return redacted, fmt.Errorf("UserMessageAppended.DecryptPII content base64: %w", b64err)
		}
		pt, err := s.DecryptField(ctx, tenantID, subject, sealed)
		switch {
		case err == nil:
			e.Content = string(pt)
		case errors.Is(err, shred.ErrShredded):
			e.Content = ""
			redacted = append(redacted, shred.RedactedField{Name: "content", Subject: subject, Reason: "shredded"})
		default:
			return redacted, fmt.Errorf("UserMessageAppended.DecryptPII content: %w", err)
		}
	}
	return redacted, nil
}

// ---- Crypto-shredding for AssistantMessageAppended (ADR 0010) ----

// PIIFields returns the proto field names of AssistantMessageAppended's
// encrypted fields. Stable across regenerations.
func (*AssistantMessageAppended) PIIFields() []string {
	return []string{
		"content",
	}
}

// Subject returns the default subject id used to key the DEK
// for this event's PII fields. Empty when the variant has no
// (es.v1.subject_field) — caller falls back to the StreamID.
func (e *AssistantMessageAppended) Subject() string {
	return e.GetUserId()
}

// EncryptPII encrypts every classification-PERSONAL+ field in
// place using s. Called by aggregate.Runtime before Codec.Encode
// when Runtime.Shredder is configured. The framework passes the
// resolved subject id; per-field (es.v1.subject) overrides are
// honored below.
//
// `bytes` fields hold raw ciphertext after encrypt; `string` fields
// classified PERSONAL+ hold base64-encoded ciphertext so the field
// remains UTF-8-valid. Fields classified
// (es.v1.data_classification) = DATA_CLASSIFICATION_SAD cause this
// method to return an error before any field is touched — SAD MUST
// NOT be persisted (PCI-DSS §3.2). See ADR 0027.
func (e *AssistantMessageAppended) EncryptPII(ctx context.Context, s *shred.Shredder, tenantID, subject string) error {
	if e.Content != "" {
		sealed, err := s.EncryptField(ctx, tenantID, subject, []byte(e.Content))
		if err != nil {
			return fmt.Errorf("AssistantMessageAppended.EncryptPII content: %w", err)
		}
		e.Content = base64.RawStdEncoding.EncodeToString(sealed)
	}
	return nil
}

// DecryptPII reverses EncryptPII. Per-field shred → RedactedField;
// base64 decode failure on string PII fields counts as a corrupt
// payload and aborts; other errors abort. Reading a SAD-classified
// field from persistence is itself corruption (it should never have
// been written) and returns the same SAD reject error.
func (e *AssistantMessageAppended) DecryptPII(ctx context.Context, s *shred.Shredder, tenantID, subject string) (shred.RedactedFields, error) {
	var redacted shred.RedactedFields
	if e.Content != "" {
		sealed, b64err := base64.RawStdEncoding.DecodeString(e.Content)
		if b64err != nil {
			return redacted, fmt.Errorf("AssistantMessageAppended.DecryptPII content base64: %w", b64err)
		}
		pt, err := s.DecryptField(ctx, tenantID, subject, sealed)
		switch {
		case err == nil:
			e.Content = string(pt)
		case errors.Is(err, shred.ErrShredded):
			e.Content = ""
			redacted = append(redacted, shred.RedactedField{Name: "content", Subject: subject, Reason: "shredded"})
		default:
			return redacted, fmt.Errorf("AssistantMessageAppended.DecryptPII content: %w", err)
		}
	}
	return redacted, nil
}

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *Conversation.
func (m *Conversation) Clone() *Conversation {
	if m == nil {
		return nil
	}
	out := &Conversation{}
	out.ConversationId = m.ConversationId
	out.UserId = m.UserId
	out.Model = m.Model
	out.SystemPrompt = m.SystemPrompt
	if len(m.Turns) > 0 {
		out.Turns = make([]*Turn, len(m.Turns))
		for i, e := range m.Turns {
			out.Turns[i] = e.Clone()
		}
	}
	out.TokensInput = m.TokensInput
	out.TokensOutput = m.TokensOutput
	out.Closed = m.Closed
	out.CloseReason = m.CloseReason
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *Conversation) View(level es.AccessLevel) *Conversation {
	if m == nil {
		return nil
	}
	out := &Conversation{}
	out.ConversationId = m.ConversationId
	out.UserId = m.UserId
	if level >= es.AccessLevelInternal {
		out.Model = m.Model
	}
	if level >= es.AccessLevelInternal {
		out.SystemPrompt = m.SystemPrompt
	}
	if len(m.Turns) > 0 {
		out.Turns = make([]*Turn, len(m.Turns))
		for i, e := range m.Turns {
			out.Turns[i] = e.View(level)
		}
	}
	out.TokensInput = m.TokensInput
	out.TokensOutput = m.TokensOutput
	out.Closed = m.Closed
	if level >= es.AccessLevelInternal {
		out.CloseReason = m.CloseReason
	}
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *Conversation) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("conversation_id", m.ConversationId),
		slog.String("user_id", m.UserId),
		slog.String("model", m.Model),
		slog.String("system_prompt", m.SystemPrompt),
		slog.Group("turns",
			slog.Int64("count", int64(len(m.Turns))),
		),
		slog.Int64("tokens_input", int64(m.TokensInput)),
		slog.Int64("tokens_output", int64(m.TokensOutput)),
		slog.Bool("closed", m.Closed),
		slog.String("close_reason", m.CloseReason),
	)
}

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *Turn.
func (m *Turn) Clone() *Turn {
	if m == nil {
		return nil
	}
	out := &Turn{}
	out.Role = m.Role
	out.Content = m.Content
	out.Tokens = m.Tokens
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *Turn) View(level es.AccessLevel) *Turn {
	if m == nil {
		return nil
	}
	out := &Turn{}
	out.Role = m.Role
	if level >= es.AccessLevelSubject {
		out.Content = m.Content
	}
	out.Tokens = m.Tokens
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *Turn) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("role", m.Role.String()),
		slog.String("content", "[REDACTED:PERSONAL]"),
		slog.Int64("tokens", int64(m.Tokens)),
	)
}

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *Start.
func (m *Start) Clone() *Start {
	if m == nil {
		return nil
	}
	out := &Start{}
	out.TenantId = m.TenantId
	out.ConversationId = m.ConversationId
	out.UserId = m.UserId
	out.Model = m.Model
	out.SystemPrompt = m.SystemPrompt
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *Start) View(level es.AccessLevel) *Start {
	if m == nil {
		return nil
	}
	out := &Start{}
	out.TenantId = m.TenantId
	out.ConversationId = m.ConversationId
	out.UserId = m.UserId
	out.Model = m.Model
	if level >= es.AccessLevelInternal {
		out.SystemPrompt = m.SystemPrompt
	}
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *Start) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("tenant_id", m.TenantId),
		slog.String("conversation_id", m.ConversationId),
		slog.String("user_id", m.UserId),
		slog.String("model", m.Model),
		slog.String("system_prompt", m.SystemPrompt),
	)
}

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *AppendUserMessage.
func (m *AppendUserMessage) Clone() *AppendUserMessage {
	if m == nil {
		return nil
	}
	out := &AppendUserMessage{}
	out.TenantId = m.TenantId
	out.ConversationId = m.ConversationId
	out.UserId = m.UserId
	out.Content = m.Content
	out.Tokens = m.Tokens
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *AppendUserMessage) View(level es.AccessLevel) *AppendUserMessage {
	if m == nil {
		return nil
	}
	out := &AppendUserMessage{}
	out.TenantId = m.TenantId
	out.ConversationId = m.ConversationId
	out.UserId = m.UserId
	if level >= es.AccessLevelSubject {
		out.Content = m.Content
	}
	out.Tokens = m.Tokens
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *AppendUserMessage) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("tenant_id", m.TenantId),
		slog.String("conversation_id", m.ConversationId),
		slog.String("user_id", m.UserId),
		slog.String("content", "[REDACTED:PERSONAL]"),
		slog.Int64("tokens", int64(m.Tokens)),
	)
}

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *AppendAssistantMessage.
func (m *AppendAssistantMessage) Clone() *AppendAssistantMessage {
	if m == nil {
		return nil
	}
	out := &AppendAssistantMessage{}
	out.TenantId = m.TenantId
	out.ConversationId = m.ConversationId
	out.UserId = m.UserId
	out.Content = m.Content
	out.Tokens = m.Tokens
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *AppendAssistantMessage) View(level es.AccessLevel) *AppendAssistantMessage {
	if m == nil {
		return nil
	}
	out := &AppendAssistantMessage{}
	out.TenantId = m.TenantId
	out.ConversationId = m.ConversationId
	out.UserId = m.UserId
	if level >= es.AccessLevelSubject {
		out.Content = m.Content
	}
	out.Tokens = m.Tokens
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *AppendAssistantMessage) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("tenant_id", m.TenantId),
		slog.String("conversation_id", m.ConversationId),
		slog.String("user_id", m.UserId),
		slog.String("content", "[REDACTED:PERSONAL]"),
		slog.Int64("tokens", int64(m.Tokens)),
	)
}

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *Close.
func (m *Close) Clone() *Close {
	if m == nil {
		return nil
	}
	out := &Close{}
	out.TenantId = m.TenantId
	out.ConversationId = m.ConversationId
	out.UserId = m.UserId
	out.Reason = m.Reason
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *Close) View(level es.AccessLevel) *Close {
	if m == nil {
		return nil
	}
	out := &Close{}
	out.TenantId = m.TenantId
	out.ConversationId = m.ConversationId
	out.UserId = m.UserId
	if level >= es.AccessLevelInternal {
		out.Reason = m.Reason
	}
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *Close) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("tenant_id", m.TenantId),
		slog.String("conversation_id", m.ConversationId),
		slog.String("user_id", m.UserId),
		slog.String("reason", m.Reason),
	)
}

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *Started.
func (m *Started) Clone() *Started {
	if m == nil {
		return nil
	}
	out := &Started{}
	out.ConversationId = m.ConversationId
	out.UserId = m.UserId
	out.Model = m.Model
	out.SystemPrompt = m.SystemPrompt
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *Started) View(level es.AccessLevel) *Started {
	if m == nil {
		return nil
	}
	out := &Started{}
	out.ConversationId = m.ConversationId
	out.UserId = m.UserId
	if level >= es.AccessLevelInternal {
		out.Model = m.Model
	}
	if level >= es.AccessLevelInternal {
		out.SystemPrompt = m.SystemPrompt
	}
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *Started) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("conversation_id", m.ConversationId),
		slog.String("user_id", m.UserId),
		slog.String("model", m.Model),
		slog.String("system_prompt", m.SystemPrompt),
	)
}

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *UserMessageAppended.
func (m *UserMessageAppended) Clone() *UserMessageAppended {
	if m == nil {
		return nil
	}
	out := &UserMessageAppended{}
	out.ConversationId = m.ConversationId
	out.UserId = m.UserId
	out.Content = m.Content
	out.Tokens = m.Tokens
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *UserMessageAppended) View(level es.AccessLevel) *UserMessageAppended {
	if m == nil {
		return nil
	}
	out := &UserMessageAppended{}
	out.ConversationId = m.ConversationId
	out.UserId = m.UserId
	if level >= es.AccessLevelSubject {
		out.Content = m.Content
	}
	out.Tokens = m.Tokens
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *UserMessageAppended) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("conversation_id", m.ConversationId),
		slog.String("user_id", m.UserId),
		slog.String("content", "[REDACTED:PERSONAL]"),
		slog.Int64("tokens", int64(m.Tokens)),
	)
}

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *AssistantMessageAppended.
func (m *AssistantMessageAppended) Clone() *AssistantMessageAppended {
	if m == nil {
		return nil
	}
	out := &AssistantMessageAppended{}
	out.ConversationId = m.ConversationId
	out.UserId = m.UserId
	out.Content = m.Content
	out.Tokens = m.Tokens
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *AssistantMessageAppended) View(level es.AccessLevel) *AssistantMessageAppended {
	if m == nil {
		return nil
	}
	out := &AssistantMessageAppended{}
	out.ConversationId = m.ConversationId
	out.UserId = m.UserId
	if level >= es.AccessLevelSubject {
		out.Content = m.Content
	}
	out.Tokens = m.Tokens
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *AssistantMessageAppended) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("conversation_id", m.ConversationId),
		slog.String("user_id", m.UserId),
		slog.String("content", "[REDACTED:PERSONAL]"),
		slog.Int64("tokens", int64(m.Tokens)),
	)
}

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *Closed.
func (m *Closed) Clone() *Closed {
	if m == nil {
		return nil
	}
	out := &Closed{}
	out.ConversationId = m.ConversationId
	out.UserId = m.UserId
	out.Reason = m.Reason
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *Closed) View(level es.AccessLevel) *Closed {
	if m == nil {
		return nil
	}
	out := &Closed{}
	out.ConversationId = m.ConversationId
	out.UserId = m.UserId
	if level >= es.AccessLevelInternal {
		out.Reason = m.Reason
	}
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *Closed) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("conversation_id", m.ConversationId),
		slog.String("user_id", m.UserId),
		slog.String("reason", m.Reason),
	)
}
