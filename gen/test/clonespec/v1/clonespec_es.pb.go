// Code generated by protoc-gen-es-go. DO NOT EDIT.
// source: test/clonespec/v1/clonespec.proto

package clonespecv1

import (
	es "github.com/laenenai/eventstore/es"
	slog "log/slog"
)

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *Inner.
func (m *Inner) Clone() *Inner {
	if m == nil {
		return nil
	}
	out := &Inner{}
	out.Name = m.Name
	if len(m.Blob) > 0 {
		out.Blob = append([]byte(nil), m.Blob...)
	}
	out.Value = m.Value
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *Inner) View(level es.AccessLevel) *Inner {
	if m == nil {
		return nil
	}
	out := &Inner{}
	out.Name = m.Name
	if len(m.Blob) > 0 {
		out.Blob = append([]byte(nil), m.Blob...)
	}
	out.Value = m.Value
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *Inner) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("name", m.Name),
		slog.Int64("blob.len", int64(len(m.Blob))),
		slog.Int64("value", int64(m.Value)),
	)
}

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *Spec.
func (m *Spec) Clone() *Spec {
	if m == nil {
		return nil
	}
	out := &Spec{}
	out.SString = m.SString
	out.SInt = m.SInt
	out.SBool = m.SBool
	out.SFloat = m.SFloat
	if len(m.SBytes) > 0 {
		out.SBytes = append([]byte(nil), m.SBytes...)
	}
	out.SMsg = m.SMsg.Clone()
	if len(m.RStrings) > 0 {
		out.RStrings = append(out.RStrings[:0:0], m.RStrings...)
	}
	if len(m.RInts) > 0 {
		out.RInts = append(out.RInts[:0:0], m.RInts...)
	}
	if len(m.RBytes) > 0 {
		out.RBytes = make([][]byte, len(m.RBytes))
		for i, b := range m.RBytes {
			if b != nil {
				out.RBytes[i] = append([]byte(nil), b...)
			}
		}
	}
	if len(m.RMsgs) > 0 {
		out.RMsgs = make([]*Inner, len(m.RMsgs))
		for i, e := range m.RMsgs {
			out.RMsgs[i] = e.Clone()
		}
	}
	if len(m.MString) > 0 {
		out.MString = make(map[string]string, len(m.MString))
		for k, v := range m.MString {
			out.MString[k] = v
		}
	}
	if len(m.MInt) > 0 {
		out.MInt = make(map[string]int64, len(m.MInt))
		for k, v := range m.MInt {
			out.MInt[k] = v
		}
	}
	if len(m.MBytes) > 0 {
		out.MBytes = make(map[string][]byte, len(m.MBytes))
		for k, v := range m.MBytes {
			if v != nil {
				out.MBytes[k] = append([]byte(nil), v...)
			}
		}
	}
	if len(m.MMsg) > 0 {
		out.MMsg = make(map[string]*Inner, len(m.MMsg))
		for k, v := range m.MMsg {
			out.MMsg[k] = v.Clone()
		}
	}
	switch v := m.Variant.(type) {
	case *Spec_OStr:
		if v != nil {
			out.Variant = &Spec_OStr{OStr: v.OStr}
		}
	case *Spec_OBytes:
		if v != nil {
			cp := &Spec_OBytes{}
			if len(v.OBytes) > 0 {
				cp.OBytes = append([]byte(nil), v.OBytes...)
			}
			out.Variant = cp
		}
	case *Spec_OMsg:
		if v != nil {
			out.Variant = &Spec_OMsg{OMsg: v.OMsg.Clone()}
		}
	}
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *Spec) View(level es.AccessLevel) *Spec {
	if m == nil {
		return nil
	}
	out := &Spec{}
	out.SString = m.SString
	out.SInt = m.SInt
	out.SBool = m.SBool
	out.SFloat = m.SFloat
	if len(m.SBytes) > 0 {
		out.SBytes = append([]byte(nil), m.SBytes...)
	}
	out.SMsg = m.SMsg.View(level)
	if len(m.RStrings) > 0 {
		out.RStrings = append(out.RStrings[:0:0], m.RStrings...)
	}
	if len(m.RInts) > 0 {
		out.RInts = append(out.RInts[:0:0], m.RInts...)
	}
	if len(m.RBytes) > 0 {
		out.RBytes = make([][]byte, len(m.RBytes))
		for i, b := range m.RBytes {
			if b != nil {
				out.RBytes[i] = append([]byte(nil), b...)
			}
		}
	}
	if len(m.RMsgs) > 0 {
		out.RMsgs = make([]*Inner, len(m.RMsgs))
		for i, e := range m.RMsgs {
			out.RMsgs[i] = e.View(level)
		}
	}
	if len(m.MString) > 0 {
		out.MString = make(map[string]string, len(m.MString))
		for k, v := range m.MString {
			out.MString[k] = v
		}
	}
	if len(m.MInt) > 0 {
		out.MInt = make(map[string]int64, len(m.MInt))
		for k, v := range m.MInt {
			out.MInt[k] = v
		}
	}
	if len(m.MBytes) > 0 {
		out.MBytes = make(map[string][]byte, len(m.MBytes))
		for k, v := range m.MBytes {
			if v != nil {
				out.MBytes[k] = append([]byte(nil), v...)
			}
		}
	}
	if len(m.MMsg) > 0 {
		out.MMsg = make(map[string]*Inner, len(m.MMsg))
		for k, v := range m.MMsg {
			out.MMsg[k] = v.View(level)
		}
	}
	switch v := m.Variant.(type) {
	case *Spec_OStr:
		if v != nil {
			out.Variant = &Spec_OStr{OStr: v.OStr}
		}
	case *Spec_OBytes:
		if v != nil {
			cp := &Spec_OBytes{}
			if len(v.OBytes) > 0 {
				cp.OBytes = append([]byte(nil), v.OBytes...)
			}
			out.Variant = cp
		}
	case *Spec_OMsg:
		if v != nil {
			out.Variant = &Spec_OMsg{OMsg: v.OMsg.View(level)}
		}
	}
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *Spec) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("s_string", m.SString),
		slog.Int64("s_int", int64(m.SInt)),
		slog.Bool("s_bool", m.SBool),
		slog.Float64("s_float", float64(m.SFloat)),
		slog.Int64("s_bytes.len", int64(len(m.SBytes))),
		slog.Any("s_msg", m.SMsg),
		slog.Group("r_strings",
			slog.Int64("count", int64(len(m.RStrings))),
		),
		slog.Group("r_ints",
			slog.Int64("count", int64(len(m.RInts))),
		),
		slog.Group("r_bytes",
			slog.Int64("count", int64(len(m.RBytes))),
		),
		slog.Group("r_msgs",
			slog.Int64("count", int64(len(m.RMsgs))),
		),
		slog.Group("m_string",
			slog.Int64("count", int64(len(m.MString))),
		),
		slog.Group("m_int",
			slog.Int64("count", int64(len(m.MInt))),
		),
		slog.Group("m_bytes",
			slog.Int64("count", int64(len(m.MBytes))),
		),
		slog.Group("m_msg",
			slog.Int64("count", int64(len(m.MMsg))),
		),
		slog.Any("variant", m.Variant),
	)
}
