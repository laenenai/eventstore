// Code generated by protoc-gen-es-go. DO NOT EDIT.
// source: test/idspec/v1/idspec.proto

package idspecv1

import (
	context "context"
	base64 "encoding/base64"
	errors "errors"
	fmt "fmt"
	aggregate "github.com/laenenai/eventstore/aggregate"
	es "github.com/laenenai/eventstore/es"
	projection "github.com/laenenai/eventstore/projection"
	shred "github.com/laenenai/eventstore/shred"
	proto "google.golang.org/protobuf/proto"
	slog "log/slog"
)

// Command is the sealed interface for the Commands sum type.
// Variants implement this via the unexported isCommand() marker method.
type Command interface {
	isCommand()
}

func (*CreateCmd) isCommand() {}
func (*UpdateCmd) isCommand() {}

// CloneSum returns a deep copy of m typed as the sealed Command interface.
// Delegates to the typed Clone(); exists so generic callers can satisfy a
// `Cloner[Command]` interface assertion (see aggregate.Runtime).
func (m *CreateCmd) CloneSum() Command { return m.Clone() }

// CloneSum returns a deep copy of m typed as the sealed Command interface.
// Delegates to the typed Clone(); exists so generic callers can satisfy a
// `Cloner[Command]` interface assertion (see aggregate.Runtime).
func (m *UpdateCmd) CloneSum() Command { return m.Clone() }

// Stable per-variant identifiers — full proto type names.
// Useful for authz, metrics, tracing, and audit annotation.
func (*CreateCmd) Action() string { return "test.idspec.v1.CreateCmd" }
func (*UpdateCmd) Action() string { return "test.idspec.v1.UpdateCmd" }

// CommandCodec encodes and decodes Command variants on the wire.
// Implements aggregate.Codec[Command].
type CommandCodec struct{}

// Compile-time assertion that the codec satisfies the framework contract.
var _ aggregate.Codec[Command] = CommandCodec{}

// Encode marshals v as proto bytes and produces an EncodedEvent
// tagged with the variant's full proto type name.
func (CommandCodec) Encode(v Command) (aggregate.EncodedEvent, error) {
	switch x := v.(type) {
	case *CreateCmd:
		b, err := proto.Marshal(x)
		if err != nil {
			return aggregate.EncodedEvent{}, err
		}
		return aggregate.EncodedEvent{
			Payload:       b,
			TypeURL:       "test.idspec.v1.CreateCmd",
			SchemaVersion: 1,
		}, nil
	case *UpdateCmd:
		b, err := proto.Marshal(x)
		if err != nil {
			return aggregate.EncodedEvent{}, err
		}
		return aggregate.EncodedEvent{
			Payload:       b,
			TypeURL:       "test.idspec.v1.UpdateCmd",
			SchemaVersion: 1,
		}, nil
	}
	return aggregate.EncodedEvent{}, fmt.Errorf("CommandCodec.Encode: unknown variant type %T", v)
}

// Decode unmarshals the proto payload back into the variant identified
// by typeURL. The schemaVersion is passed through for upcaster
// dispatch (ADR 0013); this generated codec does not perform upcasting.
func (CommandCodec) Decode(typeURL string, _ uint32, payload []byte) (Command, error) {
	switch typeURL {
	case "test.idspec.v1.CreateCmd":
		x := &CreateCmd{}
		if err := proto.Unmarshal(payload, x); err != nil {
			return nil, err
		}
		return x, nil
	case "test.idspec.v1.UpdateCmd":
		x := &UpdateCmd{}
		if err := proto.Unmarshal(payload, x); err != nil {
			return nil, err
		}
		return x, nil
	}
	return nil, fmt.Errorf("CommandCodec.Decode: unknown type_url %q", typeURL)
}

// Event is the sealed interface for the Events sum type.
// Variants implement this via the unexported isEvent() marker method.
type Event interface {
	isEvent()
}

func (*CreatedEv) isEvent()     {}
func (*RenamedEv) isEvent()     {}
func (*CleanedEv) isEvent()     {}
func (*SubjectLessEv) isEvent() {}

// CloneSum returns a deep copy of m typed as the sealed Event interface.
// Delegates to the typed Clone(); exists so generic callers can satisfy a
// `Cloner[Event]` interface assertion (see aggregate.Runtime).
func (m *CreatedEv) CloneSum() Event { return m.Clone() }

// CloneSum returns a deep copy of m typed as the sealed Event interface.
// Delegates to the typed Clone(); exists so generic callers can satisfy a
// `Cloner[Event]` interface assertion (see aggregate.Runtime).
func (m *RenamedEv) CloneSum() Event { return m.Clone() }

// CloneSum returns a deep copy of m typed as the sealed Event interface.
// Delegates to the typed Clone(); exists so generic callers can satisfy a
// `Cloner[Event]` interface assertion (see aggregate.Runtime).
func (m *CleanedEv) CloneSum() Event { return m.Clone() }

// CloneSum returns a deep copy of m typed as the sealed Event interface.
// Delegates to the typed Clone(); exists so generic callers can satisfy a
// `Cloner[Event]` interface assertion (see aggregate.Runtime).
func (m *SubjectLessEv) CloneSum() Event { return m.Clone() }

// Stable per-variant identifiers — full proto type names.
// Useful for authz, metrics, tracing, and audit annotation.
func (*CreatedEv) Action() string     { return "test.idspec.v1.CreatedEv" }
func (*RenamedEv) Action() string     { return "test.idspec.v1.RenamedEv" }
func (*CleanedEv) Action() string     { return "test.idspec.v1.CleanedEv" }
func (*SubjectLessEv) Action() string { return "test.idspec.v1.SubjectLessEv" }

// EventCodec encodes and decodes Event variants on the wire.
// Implements aggregate.Codec[Event].
type EventCodec struct{}

// Compile-time assertion that the codec satisfies the framework contract.
var _ aggregate.Codec[Event] = EventCodec{}

// Encode marshals v as proto bytes and produces an EncodedEvent
// tagged with the variant's full proto type name.
func (EventCodec) Encode(v Event) (aggregate.EncodedEvent, error) {
	switch x := v.(type) {
	case *CreatedEv:
		b, err := proto.Marshal(x)
		if err != nil {
			return aggregate.EncodedEvent{}, err
		}
		return aggregate.EncodedEvent{
			Payload:       b,
			TypeURL:       "test.idspec.v1.CreatedEv",
			SchemaVersion: 1,
		}, nil
	case *RenamedEv:
		b, err := proto.Marshal(x)
		if err != nil {
			return aggregate.EncodedEvent{}, err
		}
		return aggregate.EncodedEvent{
			Payload:       b,
			TypeURL:       "test.idspec.v1.RenamedEv",
			SchemaVersion: 1,
		}, nil
	case *CleanedEv:
		b, err := proto.Marshal(x)
		if err != nil {
			return aggregate.EncodedEvent{}, err
		}
		return aggregate.EncodedEvent{
			Payload:       b,
			TypeURL:       "test.idspec.v1.CleanedEv",
			SchemaVersion: 1,
		}, nil
	case *SubjectLessEv:
		b, err := proto.Marshal(x)
		if err != nil {
			return aggregate.EncodedEvent{}, err
		}
		return aggregate.EncodedEvent{
			Payload:       b,
			TypeURL:       "test.idspec.v1.SubjectLessEv",
			SchemaVersion: 1,
		}, nil
	}
	return aggregate.EncodedEvent{}, fmt.Errorf("EventCodec.Encode: unknown variant type %T", v)
}

// Decode unmarshals the proto payload back into the variant identified
// by typeURL. The schemaVersion is passed through for upcaster
// dispatch (ADR 0013); this generated codec does not perform upcasting.
func (EventCodec) Decode(typeURL string, _ uint32, payload []byte) (Event, error) {
	switch typeURL {
	case "test.idspec.v1.CreatedEv":
		x := &CreatedEv{}
		if err := proto.Unmarshal(payload, x); err != nil {
			return nil, err
		}
		return x, nil
	case "test.idspec.v1.RenamedEv":
		x := &RenamedEv{}
		if err := proto.Unmarshal(payload, x); err != nil {
			return nil, err
		}
		return x, nil
	case "test.idspec.v1.CleanedEv":
		x := &CleanedEv{}
		if err := proto.Unmarshal(payload, x); err != nil {
			return nil, err
		}
		return x, nil
	case "test.idspec.v1.SubjectLessEv":
		x := &SubjectLessEv{}
		if err := proto.Unmarshal(payload, x); err != nil {
			return nil, err
		}
		return x, nil
	}
	return nil, fmt.Errorf("EventCodec.Decode: unknown type_url %q", typeURL)
}

// Projection is the typed handler interface for the events in
// this package. Implementations must provide a method for every
// variant — adding a new event will break implementations at
// compile time until they decide how to handle it (return nil
// is fine for variants the projection deliberately ignores).
// See ADR 0020 decisions 3a + 3b.
type Projection interface {
	OnCreatedEv(ctx context.Context, env es.Envelope, e *CreatedEv) error
	OnRenamedEv(ctx context.Context, env es.Envelope, e *RenamedEv) error
	OnCleanedEv(ctx context.Context, env es.Envelope, e *CleanedEv) error
	OnSubjectLessEv(ctx context.Context, env es.Envelope, e *SubjectLessEv) error
}

// NewProjectionDispatcher returns a projection.Handler that decodes
// envelopes carrying this package's events and dispatches to the
// typed Projection interface. By default an event whose TypeURL is
// outside this aggregate's event set causes a non-nil error; use
// projection.IgnoreUnknown() when composing across aggregates via
// projection.Chain.
func NewProjectionDispatcher(p Projection, opts ...projection.DispatcherOption) projection.Handler {
	cfg := projection.ApplyOptions(opts)
	var codec EventCodec
	return func(ctx context.Context, env es.Envelope) error {
		if !isOurType(env.TypeURL) {
			if cfg.IgnoreUnknown {
				return nil
			}
			return fmt.Errorf("projection: unknown TypeURL %q for package test.idspec.v1", env.TypeURL)
		}
		evt, err := codec.Decode(env.TypeURL, env.SchemaVersion, env.Payload)
		if err != nil {
			return fmt.Errorf("projection: decode %s: %w", env.TypeURL, err)
		}
		switch e := evt.(type) {
		case *CreatedEv:
			return p.OnCreatedEv(ctx, env, e)
		case *RenamedEv:
			return p.OnRenamedEv(ctx, env, e)
		case *CleanedEv:
			return p.OnCleanedEv(ctx, env, e)
		case *SubjectLessEv:
			return p.OnSubjectLessEv(ctx, env, e)
		}
		return fmt.Errorf("projection: unreachable variant %T", evt)
	}
}

// isOurType reports whether the given TypeURL is one of this
// package's event variants. Used by the dispatcher to decide
// between dispatch and (skip|error) for unknown events.
func isOurType(typeURL string) bool {
	switch typeURL {
	case "test.idspec.v1.CreatedEv":
		return true
	case "test.idspec.v1.RenamedEv":
		return true
	case "test.idspec.v1.CleanedEv":
		return true
	case "test.idspec.v1.SubjectLessEv":
		return true
	}
	return false
}

// ---- Crypto-shredding for CreatedEv (ADR 0010) ----

// PIIFields returns the proto field names of CreatedEv's
// encrypted fields. Stable across regenerations.
func (*CreatedEv) PIIFields() []string {
	return []string{
		"name",
		"email",
	}
}

// Subject returns the default subject id used to key the DEK
// for this event's PII fields. Empty when the variant has no
// (es.v1.subject_field) — caller falls back to the StreamID.
func (e *CreatedEv) Subject() string {
	return e.GetId()
}

// EncryptPII encrypts every classification-PERSONAL+ field in
// place using s. Called by aggregate.Runtime before Codec.Encode
// when Runtime.Shredder is configured. The framework passes the
// resolved subject id; per-field (es.v1.subject) overrides are
// honored below.
//
// `bytes` fields hold raw ciphertext after encrypt; `string` fields
// classified PERSONAL+ hold base64-encoded ciphertext so the field
// remains UTF-8-valid. Fields classified
// (es.v1.data_classification) = DATA_CLASSIFICATION_SAD cause this
// method to return an error before any field is touched — SAD MUST
// NOT be persisted (PCI-DSS §3.2). See ADR 0027.
func (e *CreatedEv) EncryptPII(ctx context.Context, s *shred.Shredder, tenantID, subject string) error {
	if e.Name != "" {
		sealed, err := s.EncryptField(ctx, tenantID, subject, []byte(e.Name))
		if err != nil {
			return fmt.Errorf("CreatedEv.EncryptPII name: %w", err)
		}
		e.Name = base64.RawStdEncoding.EncodeToString(sealed)
	}
	if e.Email != "" {
		sealed, err := s.EncryptField(ctx, tenantID, subject, []byte(e.Email))
		if err != nil {
			return fmt.Errorf("CreatedEv.EncryptPII email: %w", err)
		}
		e.Email = base64.RawStdEncoding.EncodeToString(sealed)
	}
	return nil
}

// DecryptPII reverses EncryptPII. Per-field shred → RedactedField;
// base64 decode failure on string PII fields counts as a corrupt
// payload and aborts; other errors abort. Reading a SAD-classified
// field from persistence is itself corruption (it should never have
// been written) and returns the same SAD reject error.
func (e *CreatedEv) DecryptPII(ctx context.Context, s *shred.Shredder, tenantID, subject string) (shred.RedactedFields, error) {
	var redacted shred.RedactedFields
	if e.Name != "" {
		sealed, b64err := base64.RawStdEncoding.DecodeString(e.Name)
		if b64err != nil {
			return redacted, fmt.Errorf("CreatedEv.DecryptPII name base64: %w", b64err)
		}
		pt, err := s.DecryptField(ctx, tenantID, subject, sealed)
		switch {
		case err == nil:
			e.Name = string(pt)
		case errors.Is(err, shred.ErrShredded):
			e.Name = ""
			redacted = append(redacted, shred.RedactedField{Name: "name", Subject: subject, Reason: "shredded"})
		default:
			return redacted, fmt.Errorf("CreatedEv.DecryptPII name: %w", err)
		}
	}
	if e.Email != "" {
		sealed, b64err := base64.RawStdEncoding.DecodeString(e.Email)
		if b64err != nil {
			return redacted, fmt.Errorf("CreatedEv.DecryptPII email base64: %w", b64err)
		}
		pt, err := s.DecryptField(ctx, tenantID, subject, sealed)
		switch {
		case err == nil:
			e.Email = string(pt)
		case errors.Is(err, shred.ErrShredded):
			e.Email = ""
			redacted = append(redacted, shred.RedactedField{Name: "email", Subject: subject, Reason: "shredded"})
		default:
			return redacted, fmt.Errorf("CreatedEv.DecryptPII email: %w", err)
		}
	}
	return redacted, nil
}

// ---- Crypto-shredding for RenamedEv (ADR 0010) ----

// PIIFields returns the proto field names of RenamedEv's
// encrypted fields. Stable across regenerations.
func (*RenamedEv) PIIFields() []string {
	return []string{
		"new_name",
	}
}

// Subject returns the default subject id used to key the DEK
// for this event's PII fields. Empty when the variant has no
// (es.v1.subject_field) — caller falls back to the StreamID.
func (e *RenamedEv) Subject() string {
	return e.GetId()
}

// EncryptPII encrypts every classification-PERSONAL+ field in
// place using s. Called by aggregate.Runtime before Codec.Encode
// when Runtime.Shredder is configured. The framework passes the
// resolved subject id; per-field (es.v1.subject) overrides are
// honored below.
//
// `bytes` fields hold raw ciphertext after encrypt; `string` fields
// classified PERSONAL+ hold base64-encoded ciphertext so the field
// remains UTF-8-valid. Fields classified
// (es.v1.data_classification) = DATA_CLASSIFICATION_SAD cause this
// method to return an error before any field is touched — SAD MUST
// NOT be persisted (PCI-DSS §3.2). See ADR 0027.
func (e *RenamedEv) EncryptPII(ctx context.Context, s *shred.Shredder, tenantID, subject string) error {
	if e.NewName != "" {
		sealed, err := s.EncryptField(ctx, tenantID, subject, []byte(e.NewName))
		if err != nil {
			return fmt.Errorf("RenamedEv.EncryptPII new_name: %w", err)
		}
		e.NewName = base64.RawStdEncoding.EncodeToString(sealed)
	}
	return nil
}

// DecryptPII reverses EncryptPII. Per-field shred → RedactedField;
// base64 decode failure on string PII fields counts as a corrupt
// payload and aborts; other errors abort. Reading a SAD-classified
// field from persistence is itself corruption (it should never have
// been written) and returns the same SAD reject error.
func (e *RenamedEv) DecryptPII(ctx context.Context, s *shred.Shredder, tenantID, subject string) (shred.RedactedFields, error) {
	var redacted shred.RedactedFields
	if e.NewName != "" {
		sealed, b64err := base64.RawStdEncoding.DecodeString(e.NewName)
		if b64err != nil {
			return redacted, fmt.Errorf("RenamedEv.DecryptPII new_name base64: %w", b64err)
		}
		pt, err := s.DecryptField(ctx, tenantID, subject, sealed)
		switch {
		case err == nil:
			e.NewName = string(pt)
		case errors.Is(err, shred.ErrShredded):
			e.NewName = ""
			redacted = append(redacted, shred.RedactedField{Name: "new_name", Subject: subject, Reason: "shredded"})
		default:
			return redacted, fmt.Errorf("RenamedEv.DecryptPII new_name: %w", err)
		}
	}
	return redacted, nil
}

// ---- Crypto-shredding for SubjectLessEv (ADR 0010) ----

// PIIFields returns the proto field names of SubjectLessEv's
// encrypted fields. Stable across regenerations.
func (*SubjectLessEv) PIIFields() []string {
	return []string{
		"note",
	}
}

// Subject returns the default subject id used to key the DEK
// for this event's PII fields. Empty when the variant has no
// (es.v1.subject_field) — caller falls back to the StreamID.
func (e *SubjectLessEv) Subject() string {
	return ""
}

// EncryptPII encrypts every classification-PERSONAL+ field in
// place using s. Called by aggregate.Runtime before Codec.Encode
// when Runtime.Shredder is configured. The framework passes the
// resolved subject id; per-field (es.v1.subject) overrides are
// honored below.
//
// `bytes` fields hold raw ciphertext after encrypt; `string` fields
// classified PERSONAL+ hold base64-encoded ciphertext so the field
// remains UTF-8-valid. Fields classified
// (es.v1.data_classification) = DATA_CLASSIFICATION_SAD cause this
// method to return an error before any field is touched — SAD MUST
// NOT be persisted (PCI-DSS §3.2). See ADR 0027.
func (e *SubjectLessEv) EncryptPII(ctx context.Context, s *shred.Shredder, tenantID, subject string) error {
	if e.Note != "" {
		sealed, err := s.EncryptField(ctx, tenantID, subject, []byte(e.Note))
		if err != nil {
			return fmt.Errorf("SubjectLessEv.EncryptPII note: %w", err)
		}
		e.Note = base64.RawStdEncoding.EncodeToString(sealed)
	}
	return nil
}

// DecryptPII reverses EncryptPII. Per-field shred → RedactedField;
// base64 decode failure on string PII fields counts as a corrupt
// payload and aborts; other errors abort. Reading a SAD-classified
// field from persistence is itself corruption (it should never have
// been written) and returns the same SAD reject error.
func (e *SubjectLessEv) DecryptPII(ctx context.Context, s *shred.Shredder, tenantID, subject string) (shred.RedactedFields, error) {
	var redacted shred.RedactedFields
	if e.Note != "" {
		sealed, b64err := base64.RawStdEncoding.DecodeString(e.Note)
		if b64err != nil {
			return redacted, fmt.Errorf("SubjectLessEv.DecryptPII note base64: %w", b64err)
		}
		pt, err := s.DecryptField(ctx, tenantID, subject, sealed)
		switch {
		case err == nil:
			e.Note = string(pt)
		case errors.Is(err, shred.ErrShredded):
			e.Note = ""
			redacted = append(redacted, shred.RedactedField{Name: "note", Subject: subject, Reason: "shredded"})
		default:
			return redacted, fmt.Errorf("SubjectLessEv.DecryptPII note: %w", err)
		}
	}
	return redacted, nil
}

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *IdState.
func (m *IdState) Clone() *IdState {
	if m == nil {
		return nil
	}
	out := &IdState{}
	out.Id = m.Id
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *IdState) View(level es.AccessLevel) *IdState {
	if m == nil {
		return nil
	}
	out := &IdState{}
	out.Id = m.Id
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *IdState) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("id", m.Id),
	)
}

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *CreateCmd.
func (m *CreateCmd) Clone() *CreateCmd {
	if m == nil {
		return nil
	}
	out := &CreateCmd{}
	out.Id = m.Id
	out.Label = m.Label
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *CreateCmd) View(level es.AccessLevel) *CreateCmd {
	if m == nil {
		return nil
	}
	out := &CreateCmd{}
	out.Id = m.Id
	out.Label = m.Label
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *CreateCmd) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("id", m.Id),
		slog.String("label", m.Label),
	)
}

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *UpdateCmd.
func (m *UpdateCmd) Clone() *UpdateCmd {
	if m == nil {
		return nil
	}
	out := &UpdateCmd{}
	out.Id = m.Id
	out.NewLabel = m.NewLabel
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *UpdateCmd) View(level es.AccessLevel) *UpdateCmd {
	if m == nil {
		return nil
	}
	out := &UpdateCmd{}
	out.Id = m.Id
	out.NewLabel = m.NewLabel
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *UpdateCmd) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("id", m.Id),
		slog.String("new_label", m.NewLabel),
	)
}

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *CreatedEv.
func (m *CreatedEv) Clone() *CreatedEv {
	if m == nil {
		return nil
	}
	out := &CreatedEv{}
	out.Id = m.Id
	out.Name = m.Name
	out.Email = m.Email
	out.Region = m.Region
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *CreatedEv) View(level es.AccessLevel) *CreatedEv {
	if m == nil {
		return nil
	}
	out := &CreatedEv{}
	out.Id = m.Id
	if level >= es.AccessLevelSubject {
		out.Name = m.Name
	}
	if level >= es.AccessLevelSubject {
		out.Email = m.Email
	}
	if level >= es.AccessLevelInternal {
		out.Region = m.Region
	}
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *CreatedEv) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("id", m.Id),
		slog.String("name", "[REDACTED:PERSONAL]"),
		slog.String("email", "[REDACTED:PERSONAL]"),
		slog.String("region", m.Region),
	)
}

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *RenamedEv.
func (m *RenamedEv) Clone() *RenamedEv {
	if m == nil {
		return nil
	}
	out := &RenamedEv{}
	out.Id = m.Id
	out.NewName = m.NewName
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *RenamedEv) View(level es.AccessLevel) *RenamedEv {
	if m == nil {
		return nil
	}
	out := &RenamedEv{}
	out.Id = m.Id
	if level >= es.AccessLevelSubject {
		out.NewName = m.NewName
	}
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *RenamedEv) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("id", m.Id),
		slog.String("new_name", "[REDACTED:PERSONAL]"),
	)
}

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *CleanedEv.
func (m *CleanedEv) Clone() *CleanedEv {
	if m == nil {
		return nil
	}
	out := &CleanedEv{}
	out.Id = m.Id
	out.Reason = m.Reason
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *CleanedEv) View(level es.AccessLevel) *CleanedEv {
	if m == nil {
		return nil
	}
	out := &CleanedEv{}
	out.Id = m.Id
	out.Reason = m.Reason
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *CleanedEv) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("id", m.Id),
		slog.String("reason", m.Reason),
	)
}

// Clone returns a deep copy of m. Nil-safe (returns nil for nil m).
// Nested messages and repeated/map fields are recursively cloned.
// Faster than proto.Clone and returns the concrete type *SubjectLessEv.
func (m *SubjectLessEv) Clone() *SubjectLessEv {
	if m == nil {
		return nil
	}
	out := &SubjectLessEv{}
	out.TraceId = m.TraceId
	out.Note = m.Note
	return out
}

// View returns a deep copy of m with fields above the caller's
// access level zero-valued. Subject fields are always visible —
// they are opaque key handles, not identifying data on their own.
// Nested messages recurse at the same level. Returns nil if m is nil.
func (m *SubjectLessEv) View(level es.AccessLevel) *SubjectLessEv {
	if m == nil {
		return nil
	}
	out := &SubjectLessEv{}
	out.TraceId = m.TraceId
	if level >= es.AccessLevelSubject {
		out.Note = m.Note
	}
	return out
}

// LogValue implements slog.LogValuer. Returns the structured
// representation of m filtered at AccessLevelInternal — PII
// fields are replaced with "[REDACTED:<CLASS>]" markers, so
// slog.Info("...", "event", e) is safe by default.
func (m *SubjectLessEv) LogValue() slog.Value {
	if m == nil {
		return slog.GroupValue()
	}
	return slog.GroupValue(
		slog.String("trace_id", m.TraceId),
		slog.String("note", "[REDACTED:PERSONAL]"),
	)
}
